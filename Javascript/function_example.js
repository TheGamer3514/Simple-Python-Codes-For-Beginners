/*
 * function_example.js
 * This script defines a function to calculate the square of a number and calls it.
 */

function square(number) {
    return number * number;
}
console.log("Square of 5:", square(5));
