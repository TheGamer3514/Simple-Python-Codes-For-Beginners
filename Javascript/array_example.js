/*
 * array_example.js
 * This script creates an array and iterates over it to print each element.
 */

let fruits = ["apple", "banana", "cherry"];
for (let fruit of fruits) {
    console.log(fruit);
}
